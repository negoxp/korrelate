%div{ id: "tab-#{tab_name}"}
  .child-tabs
    %ul.tabset
      - subtabs.each do |subtab, fields|
        %li
          %a{:href => "#tab-#{tab_name}-#{subtab}"}= subtab
      
    / / tabset
    - subtabs.each do |subtab, fields|
      %div{ id: "tab-#{tab_name}-#{subtab}" }
        = render 'content_holder', fields: fields